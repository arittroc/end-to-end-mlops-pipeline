# Base URL for the FastAPI prediction endpoint (no trailing slash).
# In production this should point at the Traefik-routed domain.
# In local dev, set to http://localhost:8085/api/v1/predict or leave
# unset to use the built-in fallback.
NEXT_PUBLIC_API_URL=http://api.houseprice.local/api/v1/predict
